<h2> Welcome, <%= current_user.username %>!</h2>

<h3>Garage:</h3>
<% if @user.cars.empty? %>
    <h4>You currently don't have any cars in your garage!</h4>
<% else %>
    <% @user.cars.each do |car| %>
        <%= car.year %> <%= car.make %> <%= car.model %> <%= car.color %> <a href="/cars/<%= car.id %>/edit">Edit Car</a><br>
    <% end %>
<% end %>
<br>
<h3><a href="/cars/new">Add a car to your garage</a></h3>